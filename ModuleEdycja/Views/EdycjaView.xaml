<UserControl x:Class="ModuleEdycja.Views.EdycjaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:prism="http://prismlibrary.com/"
 prism:ViewModelLocator.AutoWireViewModel="True"
 Width="Auto" Height="Auto" Margin="10 10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Name="SelectedViewContent" prism:RegionManager.RegionName="SelectedViewRegion"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Edytuj" Command="{Binding ButtonEditCmd}" Margin="5,0" />
            <Button Content="Zapisz" Margin="5,0" IsEnabled="{Binding IsEnabled}"/>
        </StackPanel>
    </Grid>
</UserControl>
